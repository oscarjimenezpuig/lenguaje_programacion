PROGRAM
    LET contador
    SET contador 10
    LABEL retorno
    PRINT "HOLA MUNDO":NEW_LINE
    IF (GRT (GET contador) 0)
        SET contador ADD (GET contador) (OP 1)
        JUMP retorno
    END_IF
END_PROGRAM

